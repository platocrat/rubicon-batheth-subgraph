{
  "name": "rubicon-batheth-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ platocrat/rubicon-batheth-subgraph",
    "create-local": "graph create --node http://localhost:8020/ rubicon-batheth-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ rubicon-batheth-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 rubicon-batheth-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.22.2",
    "@graphprotocol/graph-ts": "^0.22.1"
  }
}